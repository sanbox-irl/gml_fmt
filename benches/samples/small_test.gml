/*



    Perfect code.
*/

while (key < number_of_buildplaces) {
    chosen_buildplace = instance_find(obj_towerbuildplace, key);
    if (place_meeting(chosen_buildplace.x, chosen_buildplace.y, obj_tower_parent)) {
        test_number = 0.3;
        chosen_building = key;
        if (0.4 == 4.0 && place_meeting(chosen_buildplace.x, chose_buildplace.y, obj_tower_parent)) {
            do {
                show_debug_message("test")
            } until (false);
        }
    }
    chosen_buildplace = instance_find_next(obj_towerbuildplace, key);
}
